---
// export interface Props {
// 	title: string;

// }
import Button from "../components/Button.astro";
const { title, img, link } = Astro.props;
---

<article>
  <h1>{title}<br /></h1>
  <div class="img_container">
    <img src={img} alt="" />
  </div>
  <!-- <a href={link}>Ze Link</a> -->
  <div class="btn_container">
    <Button btnName="read more" href={`window.location.href="ids/${link}";`} />
  </div>
</article>

<!-- <h2>Jeg er her også forneden</h2>
<p>***************************</p> -->
<!-- <script>
  document.querySelector("article").addEventListener("click", () => (window.location.href = link));
</script> -->
<style>
  article {
    /* display: flex; */
    /* flex-direction: column; */
    padding: 1rem;
    border-radius: 5px;
    aspect-ratio: 1/1;
    /* font-family: Arial, Helvetica, sans-serif; */
    /* box-sizing: border-box; */
    box-shadow: 5px 5px 5px 0px rgba(0, 0, 0, 0.25);
    transition: all 0.5s;
    background-color: white;
  }
  article:hover {
    box-shadow: 5px 5px 5px 0px rgba(0, 0, 0, 0.1);
  }
  .img_container {
    display: flex;
    justify-content: center;
    height: 75%;
  }
  img {
    width: 75%;
    height: 75%;
    object-fit: contain;
    /* aspect-ratio: 1/1; */
  }
  h1 {
    /* background: brown; */
    height: 2.4em;
  }
  .btn_container {
    display: flex;
    justify-content: end;
  }
  /* * {
    padding: 0;
    margin: 0;
  } */
</style>
