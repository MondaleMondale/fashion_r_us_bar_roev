---
// export interface Props {
// 	title: string;

// }
const { title, img, link } = Astro.props;
---

<article>
  <h1>{title}</h1>
  <img src={img} alt="" />
  <a href={link}>Ze Link</a>
</article>

<!-- <h2>Jeg er her også forneden</h2>
<p>***************************</p> -->
<!-- <script>
  document.querySelector("article").addEventListener("click", () => (window.location.href = link));
</script> -->
<style>
  article {
    /* display: flex; */
    /* flex-direction: column; */
    font-family: Arial, Helvetica, sans-serif;
    box-sizing: content-box;
    box-shadow: 5px 5px 5px 0px rgba(0, 0, 0, 0.25);
    transition: all 0.5s;
    /* background-color:red; */
  }
  article:hover {
    box-shadow: 5px 5px 5px 0px rgba(0, 0, 0, 0.1);
  }
  img {
    width: 50%;
    height: 50%;
    object-fit: contain;
    /* aspect-ratio: 1/1; */
  }
  /* a {
    text-decoration: none;
    background-color: aqua;
    width: 100%;
    height: 100%;
  } */
</style>
